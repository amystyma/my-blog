<% @posts.each do |post| %>
<article>
  <header>
    <h1 style="overflow:visible;"><%= post.title %></h1>
    <time datetime="post.created_at">
      Posted On:  <%= post.created_at.strftime('%D') %>
    </time>
  </header>
  <div class="post-body">
    <%= post.body %>
  </div>
  <br>
  <%= link_to 'Read more...', post %>
  <hr style="opacity: 0.4;">
</artice>
<% end %>